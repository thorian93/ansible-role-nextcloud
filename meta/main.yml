---
galaxy_info:
  author: Thorian93
  description: Ansible Role Nextcloud
  license: "license (MIT)"

  platforms:
    - name: Debian
      versions:
      - 9
      - 10
    - name: Ubuntu
      versions:
      - 18
      - 20

  galaxy_tags:
    - linux
    - server
    - web
    - nextcloud

dependencies:
  - { role: ansible-role-apache2 }
  - { role: ansible-role-php }
  - { role: ansible-role-certbot, when: (nextcloud_create_self_signed_cert == "false") and (nextcloud_custom_cert == "false") }
  - { role: ansible-role-mysql }
